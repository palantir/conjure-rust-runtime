type: improvement
improvement:
  description: The node selection strategy is now configurable and defaults to `PIN_UNTIL_ERROR`
    to match Dialogue.
  links:
  - https://github.com/palantir/conjure-rust-runtime/pull/47
